services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3017:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - MONGODB_URI=${MONGODB_URI}
      - MONGODB_DB=${MONGODB_DB}
    restart: always
